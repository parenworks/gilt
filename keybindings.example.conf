# gilt keybindings configuration
# Format: [context] header, then key=action lines
# Contexts: global, files, branches, commits, tags, stashes
# Lines outside a [context] header are treated as global.
# Lines starting with # are comments.
#
# Note: This file does NOT override built-in keybindings.
# It provides additional configurable actions via the custom
# command system. Use commands.conf for custom shell commands.

[global]
# Add global keybindings here

[files]
# Add file panel keybindings here

[branches]
# Add branch panel keybindings here

[commits]
# Add commit panel keybindings here
