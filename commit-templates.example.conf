# gilt commit message templates
# Format: pattern=prefix per line
# Pattern is matched against branch name (substring match).
# The prefix is pre-filled in the commit message dialog.
#
# Examples:
# feature/=feat:
# bugfix/=fix:
# hotfix/=fix:
# release/=release:
# main=chore:
